{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "nodeVersion": "24.x",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/app.html" }
  ]
}
